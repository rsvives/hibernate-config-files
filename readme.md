# Hibernate - Ficheros de configuración

El fichero `pom.xml` está contenido en la carpeta raiz.

Situar el fichero `persistence.xml` dentro de la siguiente ruta: `./src/main/resources/META-INF/persistence.xml`.
